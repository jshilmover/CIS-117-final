import React from "react";
import { HeaderView } from "./HeaderView";

export default {
  title: "ViewComponents/HeaderView",
  component: HeaderView,
};

const Template = () => <HeaderView />;

export const ExampleHeader = Template.bind({});
