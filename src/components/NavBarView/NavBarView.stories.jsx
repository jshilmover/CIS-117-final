import React from "react";
import { NavBarView } from "./NavBarView";

export default {
  title: "ViewComponents/NavBarView",
  component: NavBarView,
};

const Template = () => <NavBarView />;
export const ExampleNavBar = Template.bind({});
